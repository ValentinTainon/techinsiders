{% block _Comment_custom_content_widget %}
	{% set editor_type = form.vars.ea_crud_form.ea_field.customOptions.get('editorType')|default(null) %}

	<textarea id="editor" data-controller="ckeditor5" {% if full_name is defined and full_name is not null %} name="{{ full_name }}" {% endif %} {% if editor_type is not null %} data-editor-type="{{ editor_type }}" {% endif %} required="required">
		{% if value is defined and value is not null %}
			{{ value }}
		{% endif %}
	</textarea>
{% endblock %}
