{% block _Comment_custom_content_widget %}
	{% set editor_config_type = form.vars.attr.editor_data.editor_config_type ?? "" %}

	<textarea id="editor" {% if full_name is defined and full_name is not null %} name={{ full_name }} {% endif %} {% if editor_config_type is not null %} data-editor-config-type={{ editor_config_type }} {% endif %}>
		{% if value is defined and value is not null %}
			{{ value }}
		{% endif %}
	</textarea>
{% endblock %}
